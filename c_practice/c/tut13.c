#include <stdio.h>
int main()
{

    // printf("\"MySirg\"");
    // int x = 11, y = 6;
    // printf("Sum of %d and %d is : %d ", x, y, x + y);
    char ch;
    ch = getchar();
    printf("the ascii code of %c is %d ", ch, ch);
}